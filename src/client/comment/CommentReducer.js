import {Map} from 'immutable';

const initialState = {
  comments: Map(),
  isFetching: false
}

export default function commentReducer(state = initialState, action) {
  switch (action.type) {
    
  }
}
